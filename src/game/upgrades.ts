// Legacy upgrades - not used in new game system
export const STANDARD_UPGRADES: any[] = [];

export const getAvailableUpgrades = (gameState: any): any[] => [];

export const getUpgradeCost = (upgrade: any, tier: number): number => 0;
